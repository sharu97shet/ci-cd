from setuptools import setup, find_packages

setup(
    name="sample_texthelper",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Sharath Shet",
    description="A sample Python app for text helper functions",
)
